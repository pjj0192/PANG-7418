# PLAN - Mission 1 구현 Phase 계획

Mission 1(`docs/PRD.md`, `docs/FEATURES/*.md`)을 여러 Phase로 나눠 구현한다. 각 Phase는 그 자체로 **동작하는 SW**여야 하며(빌드/실행 가능, 화면에서 눈으로 확인 가능), Phase 종료 시 고객(사용자)이 직접 `npm run dev`로 실행해보고 피드백을 준다.

각 Phase는 이전 Phase 위에 기능을 얹는 방식으로 진행하며, 이전 Phase에서 확정된 동작은 이후 Phase에서 깨지지 않아야 한다.

---

## Phase 1 — 메인 화면

**목표**: 앱 실행 시 보이는 진입 화면 구성 (`docs/FEATURES/main.md` 기준)

**포함 기능**
- "PANG" 타이틀 표시
- 시작 버튼(클릭 또는 Enter/Space)
- 조작법 안내 문구(방향키 이동, Space 발사)
- 시작 버튼 클릭 시 Mission 1 플레이 화면으로 전환(이 시점에는 빈 화면 또는 placeholder여도 무방)

**고객 테스트 포인트**
- 앱을 처음 켰을 때 타이틀/버튼/조작법 안내가 잘 보이는지
- 시작 버튼 클릭과 Space/Enter 키 입력 둘 다로 화면 전환이 되는지
- (게임 화면은 아직 비어있어도 정상)

---

## Phase 2 — Player 이동 & Wire 발사

**목표**: 플레이어 조작의 핵심 루프 구현 (`docs/FEATURES/game_rule.md` Player/Wire 규칙)

**포함 기능**
- 방향키 좌/우로 Player 이동, 화면 좌우 경계를 벗어나지 않음
- Space 키로 Wire를 Player 위치에서 수직 위로 발사
- Wire는 화면에 동시에 1개만 존재(발사 중엔 재발사 불가)
- Wire가 화면 상단 또는 좌우 벽에 닿으면 소멸(아직 부딪힐 Bubble은 없음)

**고객 테스트 포인트**
- 좌/우 이동이 자연스럽고 화면 밖으로 나가지 않는지
- Space로 Wire가 위로 발사되고, 상단에 닿으면 사라지는지
- Wire가 남아있는 동안 재발사가 안 되는지(연사 방지 확인)

---

## Phase 3 — Bubble 물리 (충돌 없이 움직임만)

**목표**: Bubble 오브젝트의 이동/물리 구현 (`docs/FEATURES/game_rule.md` Bubble 규칙, `docs/FEATURES/mission1.md` 큰 Bubble 1~2개)

**포함 기능**
- 큰 Bubble 1~2개를 화면에 배치, 좌/우로 튕기며 이동
- 중력의 영향을 받아 낙하, 바닥/벽에 튕길 때 직전과 동일한 높이로 재상승(에너지 손실 없음)
- 화면 좌/우/상/하 경계를 벗어나지 않고 반사

**고객 테스트 포인트**
- Bubble의 움직임이 "공처럼 튕기는" 느낌인지(포물선 낙하 + 반사)
- 화면 밖으로 튀어나가거나 끼는 등 이상 동작이 없는지
- 이 Phase에서는 Wire나 Player와 부딪혀도 아무 반응 없는 것이 정상(다음 Phase에서 처리)

---

## Phase 4 — Wire ↔ Bubble 충돌, 분할, 스테이지 클리어

**목표**: 핵심 게임 루프 완성 - Bubble을 없애서 이기는 루프 (`docs/FEATURES/game_rule.md` Wire/Bubble 상호작용, 승리 조건)

**포함 기능**
- Wire가 Bubble에 명중하면 중간 크기 2개로 분할, 다시 맞으면 작은 크기 2개로 분할
- 가장 작은 Bubble이 맞으면 분할 없이 소멸
- 화면 내 모든 Bubble이 소멸되면 스테이지 클리어 처리(클리어 화면/메시지 표시)

**고객 테스트 포인트**
- Wire로 큰 Bubble → 중간 → 작은 → 소멸까지 크기 단계가 규칙대로 줄어드는지
- 분할된 Bubble들이 서로 자연스럽게 갈라져 나가는지
- 모든 Bubble을 없앴을 때 클리어 화면이 뜨는지

---

## Phase 5 — 목숨, 무적, 게임 오버

**목표**: 패배 조건과 생존 시스템 구현 (`docs/FEATURES/game_rule.md` Player 피격, 무적 규칙)

**포함 기능**
- Player가 Bubble에 접촉하면 목숨 1개 차감(초기 목숨 5개)
- 목숨 차감 시 2.5초간 무적 상태 부여(무적 중 Bubble 접촉해도 추가 피격 없음, 시각적으로 무적 상태 구분 - 깜빡임 등)
- 목숨이 0이 되면 게임 오버 화면/메시지 표시

**고객 테스트 포인트**
- Bubble에 닿았을 때 목숨이 정확히 1개만 줄어드는지
- 목숨이 준 직후 2.5초 동안은 연속으로 닿아도 추가로 안 줄어드는지(무적 시간 체감 확인)
- 목숨이 0이 됐을 때 게임 오버 화면이 뜨는지

---

## Phase 6 — 아이템 드롭

**목표**: Bubble 소멸 시 아이템 드롭 시스템 도입

**포함 기능**
- Wire에 맞아 Bubble이 소멸(분할 포함)할 때마다 20% 확률로 아이템 1개 드롭
- 드롭된 아이템은 화면에 표시되며 낙하(중력 적용 여부는 아이템 확정 시 재검토)
- 아이템 종류/효과는 placeholder 1종으로 우선 구현(획득 시 로그/표시만, 실제 효과는 종류 확정 후 별도 Phase에서 추가)
- 아이템 종류 후보 중 하나로 "Wire 연사"(지속시간 동안 동시 발사 가능 개수 증가)가 거론됨 — Phase 2에서 Wire 상태를 배열 기반으로 설계해 이 확장에 대비함(`docs/design/phase2.md` 참고)

**고객 테스트 포인트**
- Bubble을 여러 번 터뜨렸을 때 대략 5번 중 1번 꼴로 아이템이 나오는지(체감 확률 확인)
- 드롭된 아이템이 화면에 잘 보이고 자연스럽게 떨어지는지
- 이 Phase에서는 아이템을 먹었을 때 실제 효과는 없어도 정상(종류 결정 후 반영 예정)

---

## Phase 7 — 화면 전환 마무리 & 폴리싱

**목표**: 메인 ↔ 게임 화면 간 흐름 완성, Mission 1 전체 루프 마감

**포함 기능**
- 클리어/게임 오버 화면에서 메인 화면으로 복귀 가능
- 메인 → 플레이 → (클리어/게임오버) → 메인 전체 흐름이 끊김 없이 동작
- 자잘한 UI/타이밍 다듬기(요청된 피드백 반영)

**고객 테스트 포인트**
- 처음부터 끝까지(메인 → 플레이 → 클리어 또는 게임오버 → 메인) 한 사이클을 끊김 없이 플레이할 수 있는지
- 이전 Phase들에서 준 피드백이 잘 반영됐는지 최종 확인

---

## Phase 이후 범위 (Mission 1 밖)

- Block(장애물) 배치, 아이템 종류/효과 확정, 점수/랭킹, 스테이지 셀렉트, 제한 시간
